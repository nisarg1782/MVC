<?php
$order = $this->getOrderDetails();
?>

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Customer Orders</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            background-color: #f4f4f4;
        }

        .order-container {
            max-width: 90%;
            margin: auto;
            background: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h2 {
            border-bottom: 2px solid #ddd;
            padding-bottom: 10px;
            text-align: center;
        }

        .table-wrapper {
            overflow-x: auto;
            /* Enables horizontal scrolling if needed */
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
            table-layout: fixed;
            /* Ensures proper column widths */
        }

        th,
        td {
            border: 1px solid #ddd;
            padding: 12px;
            text-align: left;
            word-wrap: break-word;
            /* Ensures text wraps properly */
        }

        th {
            background-color: #f9f9f9;
        }

        .action-buttons {
            display: flex;
            gap: 5px;
        }

        .btn {
            padding: 5px 10px;
            cursor: pointer;
            border-radius: 5px;
            border: none;
            display: inline-block;
        }

        .btn-view {
            background: #007bff;
            color: white;
        }
    </style>
</head>

<body>
    <div class="order-container">
        <h2>Your Orders</h2>
        <div class="table-wrapper">
            <table>
                <thead>
                    <tr>
                        <th>Order ID</th>
                        <th>Order No</th>
                        <th>Total Amount</th>
                        <th>Coupon Code</th>
                        <th>Discount Price</th>
                        <th>Provider Name</th>
                        <th>Charge</th>
                        <th>Payment Method</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($order as $_order): ?>
                        <tr>
                            <td><?php echo $_order->getOrderId(); ?></td>
                            <td><?php echo $_order->getOrderNo(); ?></td>
                            <td><?php echo $_order->getTotalAmount(); ?></td>
                            <td><?php echo $_order->getCouponCode(); ?></td>
                            <td><?php echo $_order->getDiscountPrice(); ?></td>
                            <td><?php echo $_order->getProviderName(); ?></td>
                            <td><?php echo $_order->getCharge(); ?></td>
                            <td><?php echo $_order->getPaymentMethod(); ?></td>
                            <td class="action-buttons">
                                <a href="<?php echo $this->getUrl("customer/order_index/view")."/?order_id=".$_order->getOrderId();?>" class="btn btn-view">View</a>
                            </td>
                        </tr>
                    <?php endforeach; ?>
                </tbody>
            </table>
        </div>
    </div>
</body>

</html>