<?php
$data = $this->getData();
$filterType = $data["filter"] ?? "text"; // Default to text if filter type is missing
$label = $data["label"] ?? "Value"; // Default label if missing
?>

<!-- Minimum Value Input -->
<input type="<?php echo $filterType; ?>" 
       name="<?php echo $label; ?>_min" 
       min="1" 
       placeholder="Enter minimum <?php echo $label; ?>">

<br>

<!-- Maximum Value Input -->
<input type="<?php echo $filterType; ?>" 
       name="<?php echo $label; ?>_max" 
       min="1"       
       placeholder="Enter maximum <?php echo $label; ?>">
