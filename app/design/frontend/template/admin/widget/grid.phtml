<?php
$this->getChildHtml("toolbar");
$columns = $this->getColumns();
?>

<table border="1">
    <thead>
        <tr>
            <?php foreach ($columns as $cols): ?>
                <th><?php echo $cols["label"]; ?></th>
            <?php endforeach; ?>
        </tr>
    </thead>
    <tbody>
        <!-- Filter Row (Aligned with Columns) -->
        <tr>
            <?php foreach ($columns as $cols): ?>
                <td><?php $this->renderFilter($cols["label"]); ?></td>
            <?php endforeach; ?>
        </tr>

        <!-- Example Product Rows (Should be dynamic) -->
        <?php foreach($this->getCollection() as $collection):?>
            <?php foreach($this->getColumns() as $cols):?>
                <tr>
                    <td>
                    
                    </td>
                </tr>
                <?php endforeach;?>
                <?php endforeach;?>
        <tr>
           
        </tr>
    </tbody>
</table>
