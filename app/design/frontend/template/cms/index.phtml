<?php
$data = $this->getProducts();
$url = Mage::getBaseUrl() . "Media/catalog_media_gallery/";
?>
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <title>Product Slider</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, minimum-scale=1, maximum-scale=1" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css" />
    <style>
        body {
            background: #eee;
            font-family: Helvetica, Arial, sans-serif;
            margin: 0;
            padding: 0;
        }

        .swiper {
            width: 100%;
            height: auto;
        }

        .swiper-wrapper {
            display: flex;
            align-items: center;
        }

        .swiper-slide {
            background: #fff;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
            width: 300px;
            height: 450px;
            text-align: center;
        }

        .swiper-slide img {
            width: 100%;
            max-width: 280px;
            height: 250px;
            object-fit: cover;
            border-radius: 10px;
        }

        .product-name {
            font-size: 18px;
            font-weight: bold;
            margin-top: 10px;
            text-align: center;
            color: #333;
        }

        .product-price {
            font-size: 16px;
            color: #e74c3c;
            margin-top: 5px;
            text-align: center;
        }
    </style>
</head>

<body>
    <div class="swiper mySwiper">
        <div class="swiper-wrapper">
            <?php foreach ($data as $product) { ?>
                <div class="swiper-slide">
                    <?php $image_url = $url . $product->getFilePath(); ?>
                    <img src="<?php echo $image_url; ?>" alt="<?php echo htmlspecialchars($product->getName(), ENT_QUOTES, 'UTF-8'); ?>">
                    <div class="product-name"><?php echo htmlspecialchars($product->getName(), ENT_QUOTES, 'UTF-8'); ?></div>
                    <div class="product-price">RS. <?php echo number_format($product->getPrice(), 2); ?></div>
                </div>
            <?php } ?>
        </div>
        <div class="swiper-pagination"></div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>
    <script>
        var swiper = new Swiper(".mySwiper", {
            pagination: {
                el: ".swiper-pagination",
                dynamicBullets: true,
            },
            autoplay: {
                delay: 3000,
                disableOnInteraction: false,
            },
            loop: true,
        });
    </script>
</body>

</html>