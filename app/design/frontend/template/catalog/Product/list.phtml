<?php
$data1 = $this->productData(); // Assuming this returns an array of product objects
$mediaBaseUrl = Mage::getBaseUrl() . "Media/catalog_media_gallery/";
$catdata = $this->getCategoryData(); // Assuming this gets category data
$att_data = Mage::getModel("catalog/product_attribute")->getCollection()->addFieldToFilter(
    "attribute_id",
    ["=" => 1]
)->getData();
// echo "<pre>";
// print_r($att_data);


$unique = [];
?>
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Cards</title>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <style>
        .main-container {
            display: flex;
            gap: 20px;
            padding: 20px;
        }

        .sidebar {
            width: 250px;
            background: #f8f9fa;
            padding: 15px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .sidebar h3 {
            margin-bottom: 15px;
            font-size: 20px;
            color: #333;
        }

        .sidebar form {
            display: flex;
            flex-direction: column;
        }

        .sidebar label {
            font-size: 14px;
            margin: 5px 0;
            cursor: pointer;
        }

        .sidebar input[type="number"] {
            width: calc(50% - 5px);
            padding: 8px;
            border: 1px solid #ccc;
            border-radius: 5px;
            margin-bottom: 10px;
        }

        .sidebar button {
            background-color: #007bff;
            color: white;
            border: none;
            padding: 10px;
            border-radius: 5px;
            cursor: pointer;
            transition: background 0.3s;
        }

        .sidebar button:hover {
            background-color: #0056b3;
        }

        .product-section {
            flex: 1;
            display: flex;
            flex-direction: column;
        }

        .container {
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .row {
            display: flex;
            justify-content: center;
            gap: 20px;
        }

        .product-card {
            background: white;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 20px;
            text-align: center;
            width: 250px;
            transition: transform 0.3s ease-in-out;
        }

        .product-card img {
            width: 200px;
            height: 100px;
            border-radius: 10px;
        }

        .view-btn {
            margin-top: 10px;
            padding: 10px 15px;
            background-color: #007bff;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background 0.3s;
        }

        .product-card:hover {
            transform: translateY(-5px);
        }

        @media (max-width: 768px) {
            .main-container {
                flex-direction: column;
                align-items: center;
            }

            .sidebar {
                width: 100%;
            }
        }
    </style>

    <!-- <script>
        $(document).ready(function() {
            function filterProducts() {
                let minPrice = $("input[name='min_price']").val().trim() !== "" ? parseFloat($("input[name='min_price']").val()) : 0;
                let maxPrice = $("input[name='max_price']").val().trim() !== "" ? parseFloat($("input[name='max_price']").val()) : Infinity;
                // let selectedCategory = $("input[name='category']:checked").val(); // Get selected category

                console.log("Min Price:", minPrice, "Max Price:", maxPrice);

                $(".product-card").each(function() {
                    let priceText = $(this).find(".price").text().trim();
                    let price = parseFloat(priceText.replace("Rs. ", "").replace(",", "").trim());
                    // let productCategory = $(this).data("category"); // Get category from data attribute

                    // let priceMatch = 
                    // let categoryMatch = selectedCategory === "all" || productCategory === selectedCategory;

                    if (price >= minPrice && price <= maxPrice) {
                        $(this).show();
                    } else {
                        $(this).hide();
                    }
                });
            }

            $("#filter-form").on("submit", function(e) {
                e.preventDefault();
                filterProducts();
            });

            $("input[name='min_price'], input[name='max_price']").on("input change", function() {
                filterProducts();
            });
        });
    </script> -->
</head>

<body>
    <div class="main-container">
        <!-- Sidebar for Filters -->
        <div class="sidebar">
            <h3>Filters</h3>
            <form id="filter-form" method="GET">
                <label for="price">Price Range:</label>
                <div style="display: flex; gap: 10px;">
                    <input type="number" name="min_price" placeholder="Min">
                    <input type="number" name="max_price" placeholder="Max">
                </div>
                <h4>Select Category:</h4>
                <?php foreach ($catdata as $category): ?>
                    <label>
                        <input type="checkbox" name="cid[]" value="<?php echo $category->getCategoryId(); ?>">
                        <?php echo $category->getName(); ?>
                    </label>
                <?php endforeach; ?>
                <br>
                <h4>Select Attribute:</h4>
                <?php
                $unique = []; // Ensure $unique is initialized
                foreach ($att_data as $data):
                    $value = $data->getValue();

                    if (!in_array($value, $unique) and $value!=""):
                        $unique[] = $value; // Store the value directly in the array
                ?>
                        <?php echo $value; ?>
                        <input type="checkbox" name="color[]" value="<?php echo $value; ?>">
                <?php
                    endif;
                endforeach;
                ?>
                <button type="submit">Apply Filters</button>
            </form>
        </div>

        <!-- Product Display Section -->
        <div class="product-section">
            <div id="product-container" class="container">
                <?php
                $count = 0;
                $url = Mage::getBaseUrl();
                foreach ($data1 as $productObj):
                    $productName = htmlspecialchars($productObj->getName(), ENT_QUOTES, 'UTF-8');
                    $productPrice = number_format($productObj->getPrice(), 2);
                    $productImage = $mediaBaseUrl . $productObj->getImage();
                    $productCategory = $productObj->getCategoryId(); // Assuming product has a category ID

                    if ($count % 2 === 0) echo '<div class="row">';
                ?>
                    <div class="product-card" data-category="<?php echo $productCategory; ?>">
                        <img src="<?php echo $productImage; ?>" alt="<?php echo $productName; ?>">
                        <h3><?php echo $productName; ?></h3>
                        <p class="price">Rs. <?php echo $productPrice; ?></p>
                        <a href="<?php echo $url; ?>Catalog/Product/view/?id=<?php echo $productObj->getProductId(); ?>">
                            <button class="view-btn">View Product</button>
                        </a>
                    </div>
                <?php
                    $count++;
                    if ($count % 2 === 0) echo '</div>';
                endforeach;
                if ($count % 2 !== 0) echo '</div>';
                ?>
            </div>
        </div>
    </div>
</body>

</html>