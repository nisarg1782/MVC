<?php
$data1 = $this->productData();
$mediaBaseUrl = Mage::getBaseUrl() . "Media/catalog_media_gallery/";
// $catdata = $this->getCategoryData();
?>
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Cards</title>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <style>
    .main-container {
        display: flex;
        gap: 20px;
        padding: 20px;
    }

    .product-section {
        flex: 1;
    }

    .container {
        display: flex;
        flex-wrap: wrap;
        gap: 20px;
    }

    .product-card {
        background: white;
        border-radius: 10px;
        padding: 20px;
        text-align: center;
        width: 250px;
    }

    .product-card img {
        width: 200px;
        height: 100px;
        border-radius: 10px;
    }

    .view-btn {
        margin-top: 10px;
        padding: 10px;
        background-color: #007bff;
        color: white;
        border: none;
        border-radius: 5px;
        cursor: pointer;
    }
    </style>
    <script>
    function showdata() {
        var selectedCategories = [];
        document.querySelectorAll('input[name="category[]"]:checked').forEach(function(checkbox) {
            selectedCategories.push(checkbox.value);
        });

        var formattedCategories = selectedCategories.join(',');

        var xhr = new XMLHttpRequest();
        xhr.open("GET", "http://localhost/MVC/catalog/product/list/?cid=" + formattedCategories, true);
        xhr.onreadystatechange = function() {
            if (xhr.readyState == 4 && xhr.status == 200) {
                document.getElementById("main_body").innerHTML = xhr.responseText;


                selectedCategories.forEach(function(value) {
                    var checkbox = document.querySelector('input[name="category[]"][value="' + value +
                    '"]');
                    if (checkbox) {
                        checkbox.checked = true;
                    }
                });
            }
        };
        xhr.send();
    }
    </script>
</head>

<body id="main_body">
    <div class="main-container">
        <!-- Sidebar for Filters -->


        <!-- Product Display Section -->
        <div class="product-section">
            <div id="product-container" class="container">
                <?php foreach ($data1 as $productObj): ?>
                <div class="product-card">
                    <img src="<?= $mediaBaseUrl . $productObj->getImage(); ?>" alt="<?= $productObj->getName(); ?>">
                    <h3><?= $productObj->getName(); ?></h3>
                    <p class="price">Rs. <?= number_format($productObj->getPrice(), 2); ?></p>
                    <a href="<?= Mage::getBaseUrl(); ?>Catalog/Product/view/?id=<?= $productObj->getProductId(); ?>">
                        <button class="view-btn">View Product</button>
                    </a>
                </div>
                <?php endforeach; ?>
            </div>
        </div>
    </div>
</body>

</html>